apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-azurefile-dynamic
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: my-azurefile
  resources:
    requests:
      storage: 100Gi
